{
  "name": "shades-of-pain-netlify",
  "version": "2.0.0",
  "description": "Відтінки болю — веб-читалка на Netlify + Firebase",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo Done",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "netlify-cli": "^17.0.0"
  }
}
